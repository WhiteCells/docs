```c
/*
    // 获取内核中的未决信号集
    #include <signal.h>

    int sigpending(sigset_t *set);
        argument:
            - set: 传出参数, 内核中未决信号中的信息

        return:
            - 0: success
            - -1: error, set errno
 */
```

```c

```

