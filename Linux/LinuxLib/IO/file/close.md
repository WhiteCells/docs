```c
/*
    #include <unistd.h>

    int close(int fd);
        argument:
            - fd:

        return:
            - 0: success
            - -1: error, and errno is set appropriately.
 */
```

