### 虚拟机

虚拟机安装 [Ubuntu](https://releases.ubuntu.com/)

安装 VMware Tools

安装 ssh 服务

```shell
sudo apt install openssh-server
```

更新库

```shell
sudo apt-get update
sudo apt upgrade
```

安装 net-tools 适用 ifconfig 指令查询 ip

```shell
sudo apt install net-tools
```



### 下载 [Xshell](https://www.xshell.com/zh/free-for-home-school/) 和 [Xftp](https://www.xshell.com/zh/free-for-home-school/)

Xshell 中新建会话，协议 SSH，主机为 Ubuntu 的 ipv4

vscode 中插件下载 *Remote Development* 连接 Ubuntu，在插件 SSH config 设置 HostName(ip) 和 User(Ubuntu用户名)

在本机生成 ssh 公钥

```shell
ssh-keygen -t rsa
```

生成到本机用户名下的 `.ssh` 的 id_rsa.pub

将公钥发送到 Ubuntu，Ubuntu 里也生成公钥和私钥，

在 Ubuntu 中 .ssh 路径下创建一个文件 **authorized_keys**

```shell
cd .ssh/
vim authorized_keys
```

把本机生成的公钥复制进去，之后在 vscode 连接 Ubuntu 就不需要每次输入密码



### 安装数据库

```sh
sudo apt install mysql-server
sudo apt install mysql-client
sudo apt install libmysqlclient-dev
# 查看账号及密码
sudo cat /etc/mysql/debian.cnf
```



### 安装 gcc、g++、make

```sh
sudo apt install build-essential
```



