```cpp
#include <iostream>

void foo(int a) {
    std::cout << __STDC__             << std::endl; // 用于指示编译器是否遵循 C 标准。如果编译器遵循 C 标准，则该宏的值为 1；否则，该宏未定义。
    std::cout << __cplusplus          << std::endl; // 用于指示编译器是否正在编译 C++ 代码。如果编译器正在编译 C++ 代码，则该宏的值为一个非零值；否则，该宏未定义。
    std::cout <<  __func__            << std::endl; // 用于获取当前函数的名称作为一个字符串常量。它在 C++11 标准中引入
    std::cout <<  __FUNCTION__        << std::endl; // 用于获取当前函数的名称作为一个字符串常量。它在一些编译器中可用，但不是标准的 C++ 宏
    std::cout <<  __PRETTY_FUNCTION__ << std::endl; // 用于获取当前函数的名称，并包含了更详细的信息，如参数类型和返回类型。它在某些编译器中可用，但不是标准的 C++ 宏
    std::cout <<  __FILE__            << std::endl; // 用于获取当前源文件的名称作为一个字符串常量
    std::cout <<  __LINE__            << std::endl; // 用于获取当前代码行号
    std::cout <<  __DATE__            << std::endl; // 用于获取当前编译日期，以字符串常量的形式表示
    std::cout <<  __TIME__            << std::endl; // 用于获取当前编译时间，以字符串常量的形式表示
    std::cout <<  __TIMESTAMP__       << std::endl; // 用于获取当前编译时间戳，以字符串常量的形式表示
}

int main(int argc, char const *argv[]) {
    foo(1);
    return 0;
}

/******************************************/
1
202002
foo
foo
void foo(int)
D:\distake\code\test.cpp
77
Nov 24 2023
19:42:39
Fri Nov 24 19:41:50 2023
```

